# 저장소 작업 지침

## 구현과 독립 검증

- 기능이나 동작을 바꾸는 단계마다 테스트 전담 subagent를 별도로 두어 E2E 검사를 검토·보강하고 실행한다. 구현 담당은 코드 검토·통합과 문서를 맡는다.
- 테스트 담당의 수정 범위를 테스트 파일과 로컬 검증 자료로 제한한다. 제품 코드의 결함을 발견하면 재현 방법과 실패한 검사를 구현 담당에게 전달한다.
- 사용자가 관찰할 수 있는 결과를 명시적인 기대값으로 검사한다. 구현 함수로 기대값을 계산하거나 내부 구조를 그대로 따라 하는 검사만으로 검증을 대신하지 않는다.
- 검색·필터·데이터 갱신처럼 상태가 이어지는 흐름과 변경으로 생길 수 있는 경계를 우선한다. 기존 검사를 확인하고 실질적인 빈틈을 보강한다.
- 실패를 통과시키기 위해 기대값을 낮추거나 검사를 건너뛰지 않는다. 제품 결함과 테스트 환경·검사 자체의 문제를 구분하고 근거를 남긴다.
- 최종 소스와 테스트를 기준으로 필요한 검사를 완료한다. 검증 후 소스가 바뀌면 영향받는 검사를 다시 실행한다. 문서만 바뀐 경우에는 불필요한 E2E 재실행을 요구하지 않는다.

## 단계 보존과 실행 환경

- E2E로 정상 동작을 확인하고 관련 타입 검사·단위 검사·빌드가 통과하면, 완료한 단계의 변경을 별도 커밋으로 보존한다. 관계없는 변경은 함께 커밋하지 않는다.
- 실행한 검사, 결과, 발견한 문제와 남은 한계를 기록한다. 검증을 중단한 경우 통과한 전체 실행으로 보고하지 않는다.
- 확인용 서버는 사용자의 실행·종료 요청을 따른다. 검증용 임시 서버는 소유한 프로세스를 확인해 테스트가 끝나면 종료한다.
- 실제 수집 자료, 브라우저 화면, CPU 프로파일과 상세 실행 로그는 무시되는 `.local/`에 보관한다. 공개 회귀 검사에는 가상 자료를 사용한다.
