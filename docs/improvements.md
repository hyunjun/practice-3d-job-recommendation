# ORBIT 개선 기록

개선별로 확인한 문제, 참고 자료, 반영한 동작과 검증을 기록합니다. 원본 이력서와 공개 공고 전체 응답은 이 문서나 저장소에 포함하지 않습니다.

## 01 · 채용 조건과 원문 근거 — 2026-09-19

### 확인한 문제

- Datadog의 `Time Type: Full time`과 Anthropic의 `Location Type: On-Site`가 기존 필드 목록에서 누락되어 있었습니다.
- 직무·지원자에 따라 제한되는 비자 지원 정책이 모두 미확인으로 분류되어, 지원 문구가 있는 공고를 따로 찾기 어려웠습니다.
- Cloudflare의 수출 통제 안내에 있는 `without sponsorship for an export license`가 취업 비자 미지원으로 오분류되었습니다.
- `Full time`을 정규직으로 표시해 근무 시간과 계약 형태를 혼동할 수 있었습니다.
- 화면의 판별 결과에서 실제 근거 문장으로 연결되는 경로가 없었습니다.

### 반영한 동작

- 알려진 공고별 메타데이터를 우선하고, 필요한 경우 해당 직무를 직접 설명하는 문장만 사용합니다. 일반적인 회사 복지, 유연근무 소개, `2-Flexible`처럼 뜻이 명확하지 않은 내부 값은 추정에 사용하지 않습니다.
- 비자 조건을 지원 명시·조건부·지원 없음·미확인으로 분리했습니다. 기존의 조건부 제외 필터를 유지하면서 조건부 포함 선택지를 추가했습니다. 서로 상충하는 문구는 지원 가능으로 단정하지 않습니다.
- 수출 허가 스폰서십을 취업 비자 정책에서 제외했습니다.
- 비자·근무·고용 형태의 근거 문구와 출처 종류를 공고 상세에서 펼쳐 볼 수 있습니다. 저장한 공고에도 근거가 함께 보존되며, 예전에 저장한 공고도 계속 읽을 수 있습니다.
- 풀타임·파트타임·계약직·인턴·임시직을 구분합니다. 풀타임을 정규직 계약으로 단정하지 않습니다.
- 데이터 화면에 조건별 확인 건수를 추가했습니다.
- 모바일의 조건 입력란을 한 열로 배치해 비자 지원 필터의 조건부 포함·제외 문구가 잘리지 않도록 했습니다.
- 조건 추출을 `server/job-facts.ts`로 분리해 다른 채용 게시판 수집기에서도 재사용할 수 있게 했습니다.
- Greenhouse 급여 범위의 공식 필드명인 `currency_type`을 읽도록 보완했습니다.

### 실제 데이터 확인

2026-09-19 03:56 UTC에 조회한 10개 게시판의 동일한 응답으로 이전 동작과 개선 동작을 대조했습니다. 이후 04:08 UTC에 앱의 실제 API에서도 10개 게시판이 모두 정상 조회되는 것을 확인했습니다.

| 항목 | 이전 | 개선 후 |
| --- | ---: | ---: |
| 지도·원격 탐색에 포함된 개발 공고 | 707 | 708 |
| 조건부 비자 지원으로 분류 | 0 | 182 |
| 비자 미지원으로 분류 | 115 | 0 |
| 근무 형태 미확인 | 427 | 290 |
| 고용 형태 미확인 | 633 | 560 |

추가된 1개 공고는 공고별 원격 메타데이터를 읽게 되면서 도시 좌표가 없어도 원격 탐색에 포함된 경우입니다. 비자 미지원 115개는 수출 허가 문구에 의한 오분류였으며, 개선 후에는 비자 미확인으로 남깁니다. 수치는 이 시점의 수집 범위와 분류 결과이며 채용 시장 전체 규모나 분류 정확도를 뜻하지 않습니다.

### 참고 자료

- [Greenhouse Job Board API](https://developers.greenhouse.io/job-board.html): 공개 조회, `content=true`, 공고별 `metadata`·`offices`, 급여 범위의 `currency_type`을 확인했습니다.
- [Ashby Public Job Posting API](https://developers.ashbyhq.com/docs/public-job-posting-api): `workplaceType`, `employmentType`, `secondaryLocations`, 보상 항목별 통화·기간 구조를 확인했습니다.
- [Lever Postings API](https://github.com/lever/postings-api): `categories.commitment`, `workplaceType`, `salaryRange.interval`을 확인했습니다.

이번 개선에서 실제 수집에 연결한 소스는 기존 Greenhouse 게시판입니다. Ashby와 Lever는 수집 범위를 확장할 때 사용할 구조화된 정보와 공통 데이터 계약을 검토한 자료입니다.

### 검증

- 단위 검사: 조건부·부정·상충 문구, 수출 허가와 취업 비자의 구분, 게시판 메타데이터의 우선순위, 일반 회사 소개의 오인 방지, 계약 형태, 급여 필드, 비자 필터.
- 브라우저 검사: 조건부 포함·제외 선택, 원문 근거 펼치기, 공고 저장 후 새로고침, 저장된 근거 복원, 접근성.
- 단위 검사 38개, 기존 브라우저 시나리오 11개와 새 시나리오 1개가 통과했습니다. 실제 공개 공고를 사용한 배포 빌드에서도 데스크톱·모바일의 근거 표시, 가로 넘침과 브라우저 오류를 확인했습니다.

### 이어서 확인할 항목

- 공개 데이터 모드, 검색 조건과 선택 도시의 재방문 복원 → 개선 02에 반영.
- 회사별 조회 실패 시 이전 데이터를 보존하는 수집 구조 → 개선 03에 반영. 공고 변경·마감 이력은 별도 확인 항목입니다.
- Ashby·Lever 수집기와 도시·회사 범위의 다양성 → 개선 04에서 7개 회사 추가. 제공 도시 확장은 별도 확인 항목입니다.
- 원격 근무지의 `CAN` 같은 국가 코드와 구조화된 지역 정보 정규화 → 개선 04에 반영. 국가별 지원 가능 여부가 불명확한 지역은 계속 미확인으로 남깁니다.
- 경력 차이, 필수·우대 기술, 시민권·취업 허가 조건을 반영한 추천 집계와 평가용 공고 집합.

## 02 · 마지막 탐색 복원과 조회 상태 구분 — 2026-09-19

### 확인한 문제

브라우저에서 공개 공고 → 런던 검색 → 도시 선택 → 2D 지도로 전환한 뒤 새로고침했습니다. 공개 모드는 샘플로 돌아왔고 검색어·선택 도시·지도 보기까지 초기화되었습니다. 저장한 프로필의 기본 희망 조건 외에는 탐색을 이어갈 수 없었습니다.

프로필의 기억하기를 꺼도 편집 화면을 다시 열면 체크가 켜지는 문제도 있었습니다. 탐색 상태 저장을 추가하면서 이 선택을 무시하면 저장하지 않기로 한 희망 조건이 별도 저장 항목에 남을 수 있습니다.

### 반영한 동작

- 데이터 모드, 검색어와 모든 필터, 선택 도시와 도시·원격 탭, 지도 유형·주야간 보기, 도시 정렬을 복원합니다. 기존 프로필만 있는 경우에는 이전의 희망 조건을 사용하며, 사용자가 필터를 명시적으로 초기화한 뒤에는 그 초기화 결과를 우선합니다.
- 공개 모드로 다시 방문하면 실제 공고를 재조회합니다. 첫 응답 전에는 회사·도시 수를 `—`로 표시하고, 지도와 목록에 샘플 공고를 끼워 넣지 않습니다. 성공적으로 조회한 결과가 0개인 경우와 조회하지 못한 상태를 구분합니다.
- 연결 실패 시 공개 모드와 탐색 조건을 보존하고 재시도를 제공합니다. 조회 중에도 샘플로 전환할 수 있고, 취소한 요청의 늦은 응답이 화면을 되돌리지 않도록 합니다. 화면에 이미 있는 결과를 갱신할 때는 이전 결과를 보고 있음을 알립니다.
- 연결·저장 실패 알림은 성공 체크 대신 별도의 아이콘과 색으로 표시합니다.
- 자동 복원과 첫 조회의 재시도는 화면 안의 상태 안내를 사용합니다. 중복 알림이 모바일의 재시도 버튼을 덮지 않으며, 조회 상태 변화도 스크린리더에 전달합니다.
- 프로필 기억하기를 끄면 현재 화면의 개인 탐색 조건은 유지하지만 저장하지 않습니다. 이전에 저장한 조건은 지우며, 프로필을 다시 열었을 때도 꺼 둔 선택을 유지합니다. 프로필 삭제도 같은 조건 정리를 수행합니다.
- 데이터 모드와 표시 설정은 프로필과 별도로 유지하고, 저장한 공고·메모·도시 비교는 그대로 보존합니다. 이력서 원문이나 조회한 공고 전체를 탐색 상태에 저장하지 않고, 검색 조건을 URL이나 서버 요청에 넣지 않습니다.
- 저장 데이터의 필드를 검증하고 잘못된 옵션·사라진 도시·선택 지역 밖 도시를 복구합니다. 브라우저의 저장소 접근이 차단되어도 탐색은 계속 동작합니다.
- 공개 공고 조회와 취소 처리를 `src/hooks/useCatalog.ts`로 분리했습니다.

### 참고 자료와 판단

- [MDN localStorage](https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage): 브라우저 재방문 시 유지되며 같은 출처에 한정됩니다. 브라우저 정책에 따른 `SecurityError` 가능성을 고려해 읽기와 쓰기 실패를 처리합니다.
- [MDN sessionStorage](https://developer.mozilla.org/en-US/docs/Web/API/Window/sessionStorage): 탭 세션에 한정되어도 새로고침 시에는 유지됩니다. “이번 화면에만 적용”한 프로필과 조건을 별도 세션 저장으로 우회 보관하지 않습니다.
- [React useEffect](https://react.dev/reference/react/useEffect): 조회 요청의 정리 함수와 개발 모드의 추가 실행을 고려해 오래된 응답과 취소를 처리합니다.

새로 방문한 화면에서 상태를 읽는 방식입니다. 여러 탭에서의 실시간 동기화나 다른 브라우저·기기와의 공유는 포함하지 않습니다.

### 검증

- 기존 프로필과 새 탐색 상태의 우선순위, 필드별 오류 복구, 프로필 저장 해제·삭제, 저장소 차단을 단위 검사로 확인했습니다. 전체 단위 검사 45개가 통과했습니다.
- 기존 브라우저 시나리오 12개와 신규 시나리오 5개가 통과했습니다. 신규 5개는 배포 빌드에서도 확인했습니다. 실제 UI 조작 후 새로고침·새 탭 재방문, 공개 모드의 조회 실패·재시도·빈 결과·요청 취소, 기억하기 선택과 저장 내용, 접근성을 검증했습니다.
- 실제 API의 공개 공고 708개로 데스크톱·모바일의 런던 선택·검색어·2D 보기 복원을 확인했습니다. 원격 탭 복원도 브라우저에서 확인했습니다. 모바일의 390px·320px 너비에서 연결 실패 화면의 가로 넘침과 실행 오류가 없으며, 재시도 버튼을 바로 누를 수 있음을 확인했습니다.

## 03 · 회사별 공고 보존과 조회 상태 — 2026-09-19

### 확인한 문제

기존 수집기는 일부 회사의 조회가 실패해도 성공한 회사의 결과만으로 전체 캐시를 덮어썼습니다. 04:08 UTC의 실제 캐시를 복사해 Stripe만 HTTP 503으로 실패하고 나머지는 정상적인 빈 목록을 반환하도록 재현했습니다. Stripe의 기존 공고 92개가 사라졌고 전체 결과는 최신으로 표시되었습니다.

전체 조회가 실패하고 캐시도 없으면 반복 요청을 막지 못했습니다. 서버를 다시 시작하면 재시도 간격도 초기화되었습니다. 이전 결과의 유효기간 상한과 회사별 마지막 정상 확인 시각을 화면에서 구분할 수 없었습니다.

### 반영한 동작

- 회사별 마지막 정상 결과와 마지막 조회 시도를 분리했습니다. 일부 조회가 실패하면 해당 회사의 기존 공고만 보존하며 공고의 원래 확인 시각을 바꾸지 않습니다. 정상적인 빈 목록은 기존 공고를 제거하고, 이후 실패가 발생해도 제거된 공고를 되살리지 않습니다.
- 정상 결과는 30분 사용하며 수동 재조회는 게시판별 최소 1분 간격을 둡니다. 실패 시 기본 1분부터 지수적으로 간격을 늘리고 임의 지연을 더하되 30분 이내로 제한합니다. 공급자의 `Retry-After`가 더 늦으면 그 시각을 지킵니다.
- 캐시가 전혀 없는 실패도 기록하며 재시도 대기 시간을 디스크에 보존합니다. 동시 요청은 한 수집 작업을 공유하고 게시판 조회는 최대 4개씩 실행합니다. 완료된 작업의 자리를 다음 게시판이 바로 사용합니다.
- 마지막 정상 확인 후 24시간을 넘긴 결과는 다음 조회에서 추천 대상에서 제외합니다. 모든 결과가 만료된 경우에는 API가 이를 구분해 알리고, 브라우저에 남아 있던 이전 추천도 지웁니다. 저장한 공고는 사용자가 보관한 기록으로 유지합니다.
- 데이터 화면에 최근 조회·이전 조회 공고 수와 미확인 게시판 수를 표시했습니다. 회사별 정상 확인·실패·재시도 시각을 펼쳐 볼 수 있고, 문제가 있으면 조회 기록이 먼저 열립니다. 대기 중인 재조회 버튼은 비활성화되며 시간이 지나면 새로고침 없이 다시 사용할 수 있습니다.
- 일부 게시판의 실패 안내를 탐색·비교 화면에도 유지했습니다. 이전 공고는 목록·상세·저장 화면에서 구분하고, 도시 비교에는 이전 공고가 몇 개 포함되었는지 표시합니다.
- 게시판 조회, 수집 정책, 디스크 저장을 분리했습니다. 저장 파일과 정규화한 공고를 검증하고 임시 파일을 원자적으로 교체합니다. 이전 버전 캐시는 정상 결과를 회사별로 이전하되, 복구할 수 없는 회사별 지도 제외 건수는 미확인으로 남깁니다. 새 캐시가 손상되어도 더 오래된 캐시로 되돌아가 제거된 공고를 복원하지 않습니다.

### 참고 자료와 판단

- [MDN Retry-After](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Retry-After): 대기 시간(초)과 HTTP 날짜 형식을 모두 처리합니다.
- [RFC 5861, stale-if-error](https://www.rfc-editor.org/rfc/rfc5861.html#section-4): 실패 시 이전 결과를 제한된 기간 동안 사용하되, 오래된 결과임을 숨기지 않고 유효기간을 계속 연장하지 않는 원칙을 참고했습니다.

30분·최소 1분·최대 24시간은 이 로컬 앱에 정한 정책이며 외부 API의 보장이나 RFC의 지정값이 아닙니다. HTTP 캐시 확장을 구현한 것은 아니며 앱 API는 계속 `Cache-Control: no-store`를 사용합니다. 조회와 재시도는 사용자의 요청에 따라 실행하며 상시 백그라운드 수집은 하지 않습니다.

### 검증

- 같은 캐시와 장애 조건으로 다시 실행했을 때 Stripe 공고 92개가 모두 유지되었고 원래 확인 시각인 04:08 UTC도 보존되었습니다. 나머지 게시판의 정상적인 빈 결과는 그대로 반영되었습니다. 재현은 별도 임시 디렉터리에서 실행했습니다.
- 05:17 UTC의 실제 API에서 10개 게시판 모두 정상 조회, 개발 공고 708개, 지도 제외 공고 88개를 확인했습니다.
- 부분·전체 실패, 정상 빈 목록, 24시간 만료, 재시도 간격과 서버 재시작, 동시 요청, 손상된 데이터, 디스크 기록 실패, 이전 버전 캐시를 포함한 전체 단위 검사 59개가 통과했습니다.
- 배포 빌드가 성공했고 전체 브라우저 시나리오 21개가 통과했습니다. 새 시나리오는 이전 공고의 확인 시각·저장·도시 비교·정상 복구, 재시도 대기와 샘플 전환, 만료 후 기존 추천 제거, 320px 화면과 접근성을 확인합니다.
- 별도 임시 서버에서 만료된 캐시와 공급자의 `Retry-After: 120` 응답을 연결해 HTTP 503·만료 코드·재시도 헤더를 확인했습니다. 연속 재조회에도 게시판별 실패 횟수가 늘지 않았습니다.
- 실제 공고에 부분 장애를 모사한 화면을 1440px·390px·320px에서 확인했습니다. 조회 기록과 공고 상세의 가로 넘침 및 브라우저 실행 오류가 없었습니다.

## 04 · Ashby·Lever 수집과 출처별 조건 보존 — 2026-09-19

### 확인한 문제

- 공개 데이터가 Greenhouse의 10개 회사에 한정되어 있었습니다. 샘플에는 있던 Linear·DeepL·Spotify도 실제 공고 탐색에서는 찾을 수 없었습니다.
- 공개 탐색 모드와 공고의 출처가 모두 `greenhouse`로 고정되어 있어 다른 게시판을 추가하면 캐시·저장·CSV에서 출처를 혼동할 수 있었습니다.
- Ashby에는 목록에서 제외해야 하는 `isListed: false`, 기본·추가 근무지, 구조화된 근무·고용 형태, 여러 보상 구간이 있습니다. Lever는 페이지를 나눠 반환합니다. 게시판 응답을 그대로 하나의 목록이나 급여 범위로 취급하면 일부 공고가 누락되거나 조건이 합쳐질 수 있습니다.
- 실제 n8n 응답에는 지역별 EUR·GBP 급여 구간과 이를 합산한 요약 필드가 함께 있었습니다. 요약 숫자를 한 통화의 연봉으로 사용하면 비교가 잘못됩니다.
- 원격 근무지의 `CAN`을 인식하지 못했고, `Europe`·`AMER` 같은 지역만 명시된 공고를 해당 지역에서 살펴보기 어려웠습니다. 지역 명시는 개별 국가의 지원 가능 여부와 구분해야 합니다.
- Mistral AI의 이전 Lever 게시판은 정상적인 빈 목록을 반환했습니다. 공식 채용 페이지는 Ashby의 `mistral.ai` 게시판으로 연결되어 있었습니다.

### 반영한 동작

- Greenhouse 수집기를 분리하고 Ashby·Lever 수집기를 추가했습니다. 공개 모드는 세 출처를 함께 조회하며 회사별 수집·실패 보존·만료·재시도 정책을 공유합니다.
- Ashby에서 Linear, DeepL, n8n, Supabase, Mistral AI를, Lever에서 Spotify와 Contentsquare를 수집합니다. 기존 샘플의 22개 도시·32개 회사 시나리오는 유지했습니다.
- Ashby의 목록 비공개 공고를 제외하고 기본·추가 근무지와 국가 주소를 읽습니다. Lever는 50개씩 마지막 페이지까지 읽고 ID 중복을 제거합니다. 페이지 일부가 실패하거나 반복되면 완료된 일부만으로 기존 정상 결과를 교체하지 않습니다. EU 게시판 주소도 구분합니다.
- 공고의 구조화된 근무·고용 형태를 우선하며 원문 근거를 남깁니다. Lever의 `Permanent`는 계약 기간을 뜻하는 **기간 제한 없음**으로 표시하고 풀타임과 구분합니다.
- Ashby·Lever의 구조화된 기본 급여는 보너스·주식과 분리합니다. 지역·경력별 구간과 통화·지급 기간을 보존하며, 서로 다른 금액·통화를 하나의 연봉으로 합치지 않습니다. 시간급·월급을 임의의 근무 시간으로 연봉 환산하지 않습니다. 0–0 같은 자리 표시자는 유효한 급여로 사용하지 않습니다.
- 상세에서 **공고의 보상 조건**을 확인할 수 있습니다. 비교할 수 없는 공개 보상은 **별도 보상 조건**으로 표시하고 연봉 정렬·필터의 수치 비교·도시 중앙값에서 제외합니다. 사용자는 연봉 미공개·별도 보상 공고도 포함할지 선택할 수 있습니다. 보상 구간은 저장·재방문·CSV에도 유지됩니다.
- `CAN`과 지원하는 국가의 코드·이름, 기본·추가 주소를 정규화합니다. 구조화된 국가가 다르면 같은 이름의 도시로 연결하지 않습니다. 명시된 Europe·AMER·APAC는 지역 탐색에만 활용하고, 지원 가능한 국가로 확대 해석하지 않습니다. 원격 공고의 사무실 주소로 국가별 지원을 허용하지 않습니다.
- 데이터 화면에 출처별 대상 회사 수를 표시하고 회사별 조회 기록에도 제공자를 남깁니다. 공고 상세·저장된 원문·CSV는 실제 Greenhouse·Ashby·Lever 출처를 유지합니다.
- 탐색 모드는 `public`, 각 공고의 출처는 실제 제공자로 분리했습니다. ID는 출처·회사·원래 공고 ID로 구성합니다. 캐시도 회사·게시판·제공자·Lever 지역이 일치하는 경우만 재사용합니다.
- 기존 Greenhouse v4·v3 캐시를 v5로 이전합니다. 기존 브라우저의 Greenhouse 탐색 모드는 공개 모드로 복원하며 필터·선택 도시·지도 설정·저장 공고를 보존합니다. 새로운 캐시나 더 최신인 이전 캐시가 손상되면 오래된 결과를 되살리지 않습니다.

### 실제 데이터 확인

2026-09-19 **06:04 UTC**의 앱 API에서 17개 게시판이 모두 정상 조회되었습니다. 제공하는 22개 도시 또는 원격 탐색에 포함할 수 있는 개발 공고는 **708개 → 844개**, 대상 회사는 **10개 → 17개**로 늘었습니다. 기존 Greenhouse 공고 수는 708개로 같았으며 새 출처에서 136개가 추가되었습니다.

| 출처 | 회사 | 포함한 개발 공고 |
| --- | --- | ---: |
| Greenhouse | 기존 10개 회사 | 708 |
| Ashby | Linear 11, DeepL 10, n8n 6, Supabase 24, Mistral AI 59 | 110 |
| Lever | Spotify 25, Contentsquare 1 | 26 |
| 합계 | 17개 회사 | 844 |

| 지역의 도시 공고 | 기존 | 개선 후 |
| --- | ---: | ---: |
| 미주 | 384개 · 8개 회사 | 396개 · 11개 회사 |
| 유럽 | 165개 · 8개 회사 | 228개 · 12개 회사 |
| 아시아·태평양 | 54개 · 6개 회사 | 62개 · 7개 회사 |
| 원격 공고 | 140개 | 197개 |

지역별 도시 집계는 원격 공고를 제외합니다. 여러 지역을 명시한 공고·회사는 각각에 포함되므로 행을 더한 값은 고유 공고·회사 수와 다릅니다. 모든 수치는 사용자 프로필 필터를 적용하기 전의 수집 범위입니다.

새 공고 중 18개는 Global 원격을 명시했습니다. 지역만 확인되고 지원 국가는 미확인인 원격 공고는 기본 국가 필터에서 계속 제외합니다. n8n 공고 4개의 서로 다른 보상 구간은 원문 조건대로 보존하며 연봉 비교에 사용하지 않습니다. 위치를 확정할 수 없거나 제공 도시 밖인 개발 공고 98개는 지도에서 제외했습니다. 이 수치는 조회 시점의 결과이며 시장 전체 규모나 공고 내용의 독립적인 검증을 뜻하지 않습니다.

### 참고 자료

- [Ashby Public Job Posting API](https://developers.ashbyhq.com/docs/public-job-posting-api): 공개 GET, `isListed`, 기본·추가 주소, 근무·고용 형태, 보상 구간의 Salary·통화·지급 기간.
- [Lever Postings API](https://github.com/lever/postings-api): 페이지 조회, 공개 공고의 위치·계약 기간·급여, 전 세계·EU API 주소.
- [Mistral AI 채용 페이지](https://mistral.ai/careers): 현재 연결된 [Ashby 게시판](https://jobs.ashbyhq.com/mistral.ai) 확인.

공식 문서와 실제 공개 응답을 함께 확인했습니다. Ashby의 추가 주소는 문서 예시의 평면 구조와 실제 응답의 `postalAddress` 구조를 모두 읽습니다. 구조화된 보상 표시가 비활성화되어 있으면 해당 보상 필드를 노출하지 않습니다. 이력서나 지원서를 외부 게시판에 전송하지 않으며, 실제 공고 전체 응답은 저장소에 포함하지 않습니다.

### 검증

- 전체 단위 검사 **76개**: 출처별 원문 조건과 ID, 비공개 공고 제외, 여러 주소·동명 도시, 원격 지역과 지원 국가 구분, 여러 통화·기간의 보상, Lever 페이지 누락·중복·실패와 기존 결과 보존, 출처·지역 변경 시 캐시 무효화, v4·v3 이전.
- 배포용 빌드 성공, 전체 브라우저 시나리오 **25개 통과**. 추가한 시나리오는 기존 탐색 복원, 여러 출처의 저장·CSV, 조건별 보상 유지, 도시 연봉 집계, Permanent와 풀타임 구분, 원격 지원 국가 미확인, 320px 화면과 접근성을 확인합니다.
- 실제 844개 공고를 반환하는 배포용 서버에서 1440px·390px·320px 화면을 확인했습니다. 17개 회사의 출처·조회 기록과 n8n의 EUR·GBP 보상 구간 5개가 표시되었으며, 가로 넘침·브라우저 실행 오류·자동 WCAG A/AA 검사 위반이 없었습니다.

### 이어서 확인할 항목

- 본문에만 있는 급여의 지역·통화·지급 기간과 원문 근거는 05에서 보완했습니다. 도시별 적용 구간 선택과 더 다양한 언어·금액 표기는 추가 확인이 필요합니다.
- 필수·우대 기술, 경력과 시민권·취업 허가 조건이 추천에 미치는 영향과 평가용 공고 집합.
- 제공 도시와 게시판의 지역 편중, 실제 공고 변경·마감 기록.

## 05 · 보상 조건과 원문 근거 보존 — 2026-09-19

### 확인한 문제

- Greenhouse 본문에서 처음 발견한 급여 범위를 공고 전체의 연봉으로 사용했습니다. 실제 Cloudflare 공고 `7462801`에는 포르투갈·콜로라도·워싱턴의 세 구간이 있지만, 이전 결과는 첫 번째 EUR 54,000–91,000만 남겨 미국 근무지에서도 같은 연봉으로 비교했습니다.
- `$`의 통화를 근무지에서 추정하거나, 지급 기간이 명시되지 않은 `salary`를 연간 급여로 취급할 수 있었습니다. 금액 주변의 보너스·주식·회사 매출 문구와 기본 급여를 더 엄격히 구분할 필요가 있었습니다.
- Greenhouse 목록 요청에 보상 공개 옵션이 빠져 있었습니다. 실제 Figma 인턴 공고 `6143238004`의 구조화된 응답에는 USD 55의 시간급이 있었지만 이전 화면에서는 보상 정보를 확인할 수 없었습니다.
- 수집 방식만 바꾸면 디스크 캐시와 저장 공고에는 이전에 계산한 금액이 남습니다. 기존 메모와 지원 상태를 보존하면서 보상 판단도 함께 갱신해야 했습니다.

### 반영한 동작

- Greenhouse 목록 요청에 `pay_transparency=true`를 추가했습니다. 반환되는 모든 구간의 금액·통화·제목·설명문을 읽고, 별도 공고 조회를 추가하지 않습니다. 구조화된 구간이 없으면 본문을 확인합니다.
- 본문의 고정 금액·범위·통화 표기·지급 기간을 함께 읽고, 판단에 사용한 문장을 보존합니다. 기본 급여와 총보상을 구분하며, 보너스·주식·수당·회사 자금 관련 금액을 기본 급여에 합산하지 않습니다. 한쪽 한도만 있는 문구는 없는 경계를 만들어내지 않고 근거를 남깁니다.
- 통화나 지급 기간을 확인하지 못한 금액도 범위와 원문을 표시합니다. 근무 국가나 금액의 크기로 통화·연봉을 추정하지 않습니다. 여러 구간·지역 제한·총보상·시간급·월급은 별도 보상 조건으로 유지합니다.
- 기본 급여·통화·연간 지급이 확인되고 모든 구간이 같은 비교 범위를 나타낼 때만 연봉 정렬·필터의 수치 비교·도시 중앙값에 사용합니다. 특정 지역에 한정된 구간은 현재 도시별로 자동 선택하지 않으므로 연봉 비교에서 제외합니다.
- 공고 상세에서 구간별 적용 조건과 원문 근거를 펼칠 수 있습니다. 저장·재방문에도 유지하고 CSV에 **보상 근거** 열을 추가했습니다. 데이터 화면에는 **비교 가능한 연봉**, **보상 조건 확인 필요** 공고 수를 표시합니다.
- 보상 해석과 이전 스냅샷 재검증을 공통 모듈로 분리했습니다. 캐시의 기존 금액을 저장된 본문·구간으로 다시 확인하되 원래 조회 시각과 유효기간을 바꾸지 않습니다. 저장 공고의 메모·지원 상태·저장일도 유지합니다. 근거를 복구하지 못한 저장 금액은 **이전 기록**으로 구분해 남깁니다.

### 참고 자료와 판단

- [Greenhouse Job Board API](https://developers.greenhouse.io/job-board.html): `pay_transparency=true`, `pay_input_ranges`의 센트 단위 경계·통화·제목·설명문. 지급 기간 전용 필드가 없어 제목과 설명문을 함께 확인합니다.
- [Google JobPosting 구조화된 데이터](https://developers.google.com/search/docs/appearance/structured-data/job-posting): 고용주가 제공한 기본 급여, 통화, 시간·일·주·월·연 단위, 고정 금액과 범위의 구분.
- [Schema.org baseSalary](https://schema.org/baseSalary): 기본 급여와 그 밖의 보상을 분리하는 기준.

Greenhouse 문서는 단일 공고 조회의 보상 옵션을 설명하며, 목록 API의 동작은 실제 응답으로 확인했습니다. 조사한 Figma 목록 152개 중 99개에 구조화된 보상 구간이 있었습니다. 목록에서 이 필드가 항상 반환된다고 가정하지 않으며 본문 경로를 유지합니다.

### 실제 데이터 확인

2026-09-19 **06:19 UTC**에 받은 Greenhouse 10개 게시판의 같은 응답을 이전·개선 후 수집 방식에 각각 적용했습니다. 포함한 공고 ID와 수는 모두 같았습니다.

| 비교 항목 | 이전 | 개선 후 |
| --- | ---: | ---: |
| 포함한 개발 공고 | 708 | 708 |
| 금액을 확인할 수 있는 공고 | 383 | 413 |
| 연봉 수치 비교에 사용하는 공고 | 383 | 229 |

보상 정보를 볼 수 있는 공고는 30개 늘었습니다. 연봉 비교 대상이 줄어든 것은 지역·지급 기간·기본 급여 여부를 확인하는 기준을 바꾼 결과입니다. 감소한 수를 오류 건수나 정확도 개선율로 해석하지 않습니다.

Cloudflare 사례는 세 구간과 각 적용 조건을 모두 남깁니다. EUR 구간과 달리 `$`만 적힌 두 구간은 통화를 미확인으로 표시하며 원문에서 확인할 수 있습니다. Figma 사례는 USD 55의 시간급과 게시판의 설명문을 표시하며 연봉으로 환산하지 않습니다.

**06:43 UTC**에 앱 API를 확인했을 때 17개 게시판 모두 정상 상태였고, 844개 공고 중 427개에 보상 구간·근거가 있었습니다. 비교 가능한 연봉은 231개, 별도 보상 조건 확인이 필요한 공고는 196개였습니다. 앞선 조회에서 Intercom이 시간 초과되었을 때는 기존 33개 공고와 원래 조회 시각을 보존했고, 재시도 대기 후 정상 복구되었습니다. 수치는 해당 시점의 수집 범위이며 실제 공고는 계속 바뀝니다.

### 검증

- 전체 단위 검사 **97개 통과**. 금액 표기와 구두점, 명시·미확인 통화와 지급 기간, 지역별 구간, 총보상·보너스·주식, 고정 금액과 한쪽 한도, Greenhouse 구조화된 시간급, 기존 캐시·저장 공고의 재검증과 원래 조회 시각 보존을 확인했습니다.
- 배포용 빌드 성공, 전체 브라우저 시나리오 **28개 통과**. 보상 원문·저장·지원 상태·메모·CSV·재방문, 기존 기록의 보존, 연봉 중앙값에서 비교 불가능한 금액 제외, 320px 화면과 접근성을 확인했습니다.
- 실제 Cloudflare·Figma 공고와 데이터 품질 집계를 배포용 서버의 **1440px·390px·320px** 화면에서 확인했습니다. 가로 넘침·브라우저 실행 오류·자동 WCAG A/AA 검사 위반이 없었습니다.

### 이어서 확인할 항목

- 지역·경력별 보상 구간을 사용자의 조건에 맞게 선택하는 규칙과 근거. 현재는 모든 근무지에 적용되는 것으로 확대하지 않고 별도로 표시합니다.
- 영어 중심의 본문 해석이 다루지 못하는 언어·숫자 표기와 더 다양한 실제 공고의 평가 집합.
- 필수·우대 기술과 경력 해석은 06에서 보완했습니다. 시민권·취업 허가 조건의 추천 반영, 공고 변경·마감 기록은 이어서 확인합니다.

## 06 · 필수·우대 조건을 구분하는 추천 근거 — 2026-09-19

### 확인한 문제

- 이력서와 공고에 같은 기술·경력 추출기를 사용했습니다. 회사 소개·업무 설명·자격 요건·우대 사항을 구분하지 않아 등장한 모든 기술을 같은 비중으로 비교했습니다.
- 실제 Figma 공고 `5976930004`에서 회사 이름이 `Figma` 기술 요구사항으로 잡혔습니다. 샘플 프로필에는 이 기술이 없다는 주의 문구까지 표시했습니다.
- MongoDB 공고 `8143980`의 자격 항목인 Java·SQL과 우대 항목인 Python·AWS가 같은 비중이었습니다. 우대 기술이 더 나열될수록 기술 일치 비율이 낮아지고, 선택 가능한 언어도 모두 필요한 것처럼 보일 수 있었습니다.
- Stripe 공고 `8014859`에는 필수 경력 3년과 우대 경력 5년이 있지만 가장 큰 숫자를 사용해 **최소 5년**으로 표시했습니다. 학력별로 달라지는 경력이나 기술·직무별 경험도 전체 경력 하나로 충족했다고 표현할 수 있었습니다.

### 반영한 동작

- 공고 전용 기술·경력 해석기를 분리했습니다. `Minimum requirements` 같은 필수 명시, `Who you are` 같은 일반 자격 항목, 우대 사항, 업무·본문 언급을 나눕니다. 회사 소개·복지·보상 안내의 문구가 자격 요건으로 이어지지 않도록 구간을 구분합니다.
- 필수·자격 항목의 기술 일치를 중심으로 정렬하고 확인한 우대 경험에는 제한된 추가 점수만 부여합니다. 우대 기술이 없으면 필수 부족으로 표시하지 않으며, 우대 기술이 추가로 나열됐다는 이유로 점수가 내려가지 않습니다. 업무에서만 언급한 기술은 검색에 남기되 추천 점수에는 사용하지 않습니다.
- `Python 또는 Java`처럼 분명한 선택 관계는 하나를 확인하면 다른 언어를 부족하다고 표시하지 않습니다. 필수·선택·예시 관계가 섞여 있으면 하나의 AND·OR 조건으로 단순화하지 않고 원문 확인을 남깁니다. 반복된 기술과 React Native·React, 프레임워크 이름의 `.js`로 인한 중복 계산도 줄였습니다.
- 요구 경력과 우대 경력, 연·월 단위를 따로 읽고 원문을 보존합니다. 입사 후 목표 기간·회사 연혁·학업 기간을 요구 경력으로 사용하지 않습니다. 학력·분야·선택 경로에 따라 연수가 달라지면 단일 하한 비교를 하지 않습니다.
- 입력한 전체 경력과 공고의 연수 하한을 비교하되, 기술·직무별 경력이나 학력까지 충족했다고 표현하지 않습니다. 공고 상세에서 필수·우대 연수와 각 원문을 펼쳐 볼 수 있습니다.
- 공고 카드와 저장 목록에서 필수·자격·우대 중 어떤 기술이 연결되는지 표시합니다. 상세에는 분류별 기술, 원문, 선택 관계를 추가했고, 데이터 화면에는 **자격 항목의 기술**, **경력 조건의 원문**을 확인할 수 있는 공고 수를 표시했습니다.
- 기존 캐시·저장 공고의 파생 기술과 경력을 보관된 본문으로 다시 확인합니다. 공고 ID·원래 조회 시각·저장일·메모·지원 상태는 유지하며, 재해석한 시각을 최신 수집 시각으로 사용하지 않습니다. CSV에도 기술 조건·경력 조건·원문 근거를 남깁니다.

### 참고 자료와 판단

- [Schema.org qualifications](https://schema.org/qualifications)·[skills](https://schema.org/skills): 직무의 자격과 개인·조직이 보유하거나 직무에서 요구·선호하는 역량은 같은 의미가 아닙니다.
- [Google JobPosting 경력 요구사항](https://developers.google.com/search/docs/appearance/structured-data/job-posting): 경력 개월 수와 복합 조건의 AND·OR 차이, 학력 대신 인정하는 경력. 전체 본문에서 가장 큰 연수 하나를 고르는 방식으로는 이 차이를 보존할 수 없습니다.
- [Schema.org experienceRequirements](https://schema.org/experienceRequirements): 해당 직무에 필요한 경력의 설명.
- [Stripe 사례](https://stripe.com/jobs/search?gh_jid=8014859), [Anthropic 사례](https://job-boards.greenhouse.io/anthropic/jobs/4949336008): 실제 `Minimum requirements`·`Preferred qualifications`·`Strong candidates may also have` 문구를 확인했습니다. Greenhouse·Ashby·Lever의 제목과 본문 구조를 함께 검증했습니다.

이 자료들은 데이터의 의미를 구분하는 기준입니다. 현재 정렬 규칙은 탐색을 돕기 위한 로컬 규칙이며 채용 성공률이나 검증된 최적 가중치를 뜻하지 않습니다. 이력서 처리에는 외부 분석 API를 추가하지 않았습니다.

### 실제 데이터 확인

2026-09-19 **07:13 UTC**의 배포용 앱 API에서 17개 게시판이 모두 정상 조회되었고, 공고는 844개였습니다.

| 확인할 수 있는 정보 | 공고 수 |
| --- | ---: |
| 필수·일반 자격 항목의 기술 | 555 |
| 우대 항목의 기술 | 266 |
| 경력 연수와 원문 | 464 |
| 단일 연수 하한을 비교할 수 있는 경력 | 350 |

항목은 서로 겹치므로 합산하지 않습니다. 이 수치는 문구에서 확인한 정보의 범위이며 분류 정확도나 전체 채용 시장의 비율을 뜻하지 않습니다.

- Stripe 사례는 **필수 3년·우대 5년**으로 분리되며, 상단 경력 표시는 3년입니다.
- Figma 사례의 기술은 **Python·SQL**로 표시되고 회사 이름으로 인한 기술 부족 안내가 사라졌습니다. SQL과 예시 언어의 혼합된 관계는 원문 확인을 남깁니다.
- MongoDB 사례는 **자격 항목 4개 기술·우대 7개 기술**로 구분됩니다. 샘플 프로필의 Python·AWS는 우대 경험으로 표시하고 Java·SQL 등은 별도로 확인할 자격 항목으로 남깁니다.

기존 844개 공고의 동일한 본문에도 다시 적용해 공고 ID와 보상 데이터가 유지되는 것을 확인했습니다. 기술·경력 분류는 약 0.5초에 처리됐으며 이는 이 로컬 환경에서 확인한 한 번의 실행 시간입니다.

### 검증

- 전체 단위 검사 **131개 통과**. 회사 소개와 자격·우대 구분, 선택 가능한 언어, 미확인 혼합 관계, 우대 추가에 따른 점수 불이익 방지, 원문 보존, 요구·우대 연수, 학력별 경로, 월 단위·입사 후 목표·회사 연혁, 세 수집기의 본문 구조와 캐시 이전을 확인했습니다.
- 배포용 빌드 성공, 전체 브라우저 시나리오 **32개 통과**. 추가한 시나리오는 실제 화면의 정렬·조건별 표시·원문 열람·저장·메모·지원 상태·재방문·CSV·기존 기록 이전과 320px 접근성을 확인합니다.
- 실제 Stripe·MongoDB 공고를 **1440px·390px·320px**, Figma 공고와 데이터 품질 집계를 390px 화면에서 확인했습니다. 가로 넘침·브라우저 실행 오류·자동 WCAG A/AA 검사 위반이 없었습니다.

### 이어서 확인할 항목

- 현재 사전에 없는 기술과 더 다양한 언어·문장 구조, 복합적인 기술 선택 관계. 전체 요건을 빠짐없이 추출했다고 가정하지 않으며 원문을 함께 제공합니다.
- 시민권·취업 허가·보안 인가 조건을 비자 스폰서십과 구분하는 방식, 실제 사용자 기준 추천 품질 평가.
- 공고 변경·마감 기록과 저장한 공고의 현재 게시 상태 확인.

## 07 · 저장한 공고의 현재 게시 여부와 내용 비교 — 2026-09-19

### 확인한 문제

- 저장 목록은 저장 당시 공고·메모·지원 상태만 보여주어, 나중에 다시 볼 때 현재 공개 게시 여부와 내용 차이를 확인하기 어려웠습니다.
- 기존 카탈로그는 직무·도시 필터를 통과한 공고만 보관했습니다. 이 목록에서 빠졌다는 사실만으로 공개 게시가 종료됐다고 판단하면, 직무나 근무지 해석이 달라진 공고까지 종료로 오인할 수 있습니다.
- 게시판 조회 실패, 오래된 캐시, 정상 조회의 빈 목록도 의미가 다릅니다. 전체 공개 목록의 근거 없이 같은 상태로 표시하면 저장한 지원 기록을 잘못 정리하게 할 수 있습니다.

### 반영한 동작

- Greenhouse·Ashby·Lever에서 직무·근무지 필터 전 전체 공개 ID를 확보합니다. Ashby의 목록 비공개 공고는 제외하고, Greenhouse의 제공된 전체 건수를 검증하며, Lever는 모든 페이지 조회가 성공했을 때만 반영합니다.
- 저장 목록에 **게시 상태 확인**, 확인 시각, 게시 여부·내용 차이의 집계와 필터를 추가했습니다. 포지션·근무지·근무/고용/비자·보상·기술/경력·본문·지원 링크 중 저장 내용과 다른 항목을 표시하며 현재 원문으로 연결합니다.
- **게시 확인**, **공개 목록에서 미확인**, **현재 상태 확인 필요**, **미조회**를 구분합니다. 조회 실패, 30분을 지난 확인, 전체 공개 ID가 없는 이전 캐시, 다른 게시판·출처·Lever 지역, 저장 공고보다 이전에 조회한 목록으로는 게시 종료나 현재 게시 여부를 단정하지 않습니다.
- 정상 응답이 빈 목록이면 목록에서 미확인으로 표시하지만, 이후 조회가 실패하면 확인 필요로 바뀝니다. 이전 공고를 되살리거나 저장한 기록을 삭제하지 않습니다.
- 서버의 고정된 공개 게시판 목록을 받아 브라우저에서 비교합니다. 저장 ID·메모·프로필을 전송하지 않고, 저장 공고·저장일·메모·지원 상태·탐색 모드·필터를 그대로 유지합니다.
- 표시 항목별 SHA-256으로 비교하며 수집 시각·오래된 결과 표시·추천 점수·배열 순서·서식 공백·파서 버전은 제외합니다. 게시판에는 있지만 현재 지도 범위 밖인 공고는 게시 여부만 확인하고 내용 비교는 미확인으로 남깁니다.
- 게시 상태는 현재 화면에서만 유지합니다. 열어 둔 화면에서도 확인 근거가 30분을 지나면 확인 필요로 바뀌며, 재방문 시 다시 확인합니다. CSV의 지원 상태와 공개 게시 상태를 분리하고 확인 시각·내용 일치/차이/미확인·달라진 항목을 함께 남깁니다.
- 카탈로그와 게시 상태 조회가 같은 수집·캐시·진행 중인 요청을 공유합니다. 회사별 최소 재조회 간격, 장애 재시도·`Retry-After`, 재시작 후 대기 시각, 원래 조회 시각과 24시간 보존 한도를 유지합니다. 공개 목록이 없는 기존 캐시를 빈 목록으로 간주하지 않습니다.

### 참고 자료와 판단

- [Greenhouse Job Board API](https://developers.greenhouse.io/job-board.html): 회사의 공개된 공고 목록과 전체 건수.
- [Ashby Public Job Posting API](https://developers.ashbyhq.com/docs/public-job-posting-api): 현재 게시된 공고와 `isListed`의 목록 표시 여부.
- [Lever Postings API](https://github.com/lever/postings-api): 공개 공고의 전체 페이지 조회.
- [Google JobPosting의 공고 제거](https://developers.google.com/search/docs/appearance/structured-data/job-posting#remove-job-posting): 만료된 `validThrough`, 404/410 응답, 구조화된 데이터 제거 등 여러 처리 방식.

원문 페이지의 HTTP 200 응답은 현재 모집 중이라는 증거로 사용하지 않습니다. 현재 기능은 최근 공개 목록에서 확인한 사실을 보여주며, 채용 종료·재게시·회사 이동의 원인을 추정하지 않습니다. 표시 내용의 차이도 실제 원문 변경과 정보 해석 방식의 변화를 구분할 수 없어 **저장 내용과 차이**라고 안내합니다.

### 실제 데이터 확인

2026-09-19 **07:39 UTC**의 앱 API에서 17개 게시판이 모두 정상 조회되었습니다.

| 확인 항목 | 결과 |
| --- | ---: |
| 전체 공개 ID | 3,706개 |
| 표시 내용 비교 정보를 제공하는 탐색 대상 공고 | 844개 |
| 게시 상태 응답 크기 | 739,714바이트 |

전체 ID에는 지도 범위 밖이거나 개발 직무가 아닌 공고도 포함됩니다. 이 공고의 상세 내용을 별도로 저장하거나 추천에 추가한 것은 아닙니다. 결과 수치는 해당 시점의 대상 게시판만 나타냅니다.

**07:49 UTC**의 배포용 서버에서도 같은 범위가 정상 조회되었고, 844개 카탈로그 공고의 표시 항목별 비교 값이 게시 상태 API의 값과 모두 일치했습니다. 실제 Stripe 공고의 게시 확인과 함께 내용 차이·목록 미확인·이전 게시판을 나타내는 합성 저장 기록으로 화면을 확인했습니다. 합성 사례는 실제 공고의 변경·종료 사실을 뜻하지 않습니다.

### 검증

- 전체 단위 검사 **145개 통과**. 직무·지도 범위 밖 ID와 목록 비공개 공고의 구분, 전체 페이지·응답 건수, 동시 요청 통합, 성공한 빈 목록, 장애·재시작·24시간 만료, 기존 캐시, 다른 게시판·출처·지역, 저장 시각과 비교 내용의 일관성을 확인했습니다. 서버와 브라우저의 언어별 정렬 방식이 달라도 비교 값이 같도록 고정한 순서를 사용합니다.
- 배포용 빌드 성공, 전체 브라우저 시나리오 **36개 통과**. 마지막 비교 순서 수정 후 게시 상태 시나리오 **4개**를 다시 확인했습니다. 명시적 조회, 미조회·게시 확인·차이·미확인·장애 상태, 필터·원문·CSV, 대기 시각·확인 만료·복구, 저장 기록과 탐색 조건 보존을 확인합니다.
- 게시 상태 확인 요청에는 `/api/posting-status?refresh=1`만 사용하며 저장 공고 ID·메모·프로필을 보내지 않는 것을 확인했습니다. 조회 전후 저장일·공고·메모·지원 상태·프로필·탐색 조건이 같고, 재방문 시 게시 상태만 미조회로 돌아갑니다.
- 실제 API와 합성 저장 사례를 **1440px·390px·320px** 저장 목록, **320px** 상세 화면에서 확인했습니다. 가로 넘침·브라우저 실행 오류·자동 WCAG A/AA 검사 위반이 없었습니다.

### 이어서 확인할 항목

- 현재 기능은 한 번 확인한 공개 목록과 저장 내용의 비교입니다. 게시 종료 확정, 공고의 전체 변경 이력, 재게시·게시판 이전 추적이나 자동 알림은 제공하지 않습니다.
- 전체 원문 대신 앱이 보관한 표시 내용의 범위에서 비교합니다. 지도 범위 밖 공고의 본문 비교, 언어별 정규화 품질과 원문 변경·재해석의 구분은 추가 검토가 필요합니다.
- 대상 게시판의 수가 늘어날 때 전체 공개 목록 응답의 크기와 수집 시간을 측정하고, 개인 정보 전송 없이 응답을 나눠 받을 수 있는 구조를 검토합니다.

## 08 · 비자 지원 범위와 추가 취업 자격 구분 — 2026-09-19

### 확인한 문제

- n8n의 개발 공고 6개에는 “독일에는 비자를 지원할 수 있지만 다른 국가에서는 기존 취업 권한이 필요하다”는 문구가 있었습니다. 기존 해석은 이를 일반적인 **비자 지원 명시**로 분류해 국가별 제한이 있는 공고까지 조건부 제외 필터에 포함했습니다.
- 원격근무 국가와 프로필의 거주 국가가 일치하면 “원격 지원이 가능하다”고 표시했습니다. 그러나 실제 공고에는 시민권, 취업 허가, 주별 거주 제한이나 보안 인가 등의 별도 조건이 있어 지리적 일치만으로 지원 자격을 확인할 수 없습니다.
- Anthropic의 `Staff+ Software Engineer, Public Sector` 공고는 시민권 확인을 필수로 명시하면서 보안 인가는 우대 항목에 적었습니다. 시민권의 증명 방법으로 정부 보안 인가를 예시한 문장도 있어, 관련 단어가 등장했다는 이유만으로 모든 보안 인가를 필수로 취급하면 안 됩니다.
- Cloudflare의 수출 허가 스폰서십과 취업 비자, 수출 통제의 `U.S. person`과 미국 시민권도 서로 구분해야 합니다.

### 반영한 동작

- 특정 국가·직무·지원자 범위에 한정된 비자 지원을 **조건부 지원**으로 표시합니다. “조건부 제외” 필터에서는 제외하며 조건부 포함 탐색에서는 원문과 함께 확인할 수 있습니다. 지도에 등록되지 않은 국가나 도시의 범위도 버리지 않습니다.
- 취업 자격을 비자 지원 적용 범위·기존 취업 허가·국적과 시민권·거주 요건·보안 인가·수출 통제로 구분했습니다. 각 문구에 **필수로 명시·적용 조건 확인·우대 사항·원문 확인**과 원문 근거를 남깁니다.
- 공고 상세에 **취업 자격과 추가 조건**을 추가했습니다. 같은 문구에서 확인한 조건은 묶어 펼쳐 볼 수 있고, 목록·저장 카드와 추천 설명에서도 별도 조건을 확인하도록 안내합니다.
- 우대 보안 인가, 보유하거나 취득할 수 있다는 선택 경로, 시민권의 증명 수단을 구분합니다. 차별 금지 문구, 개인정보 수집 목록, 소프트웨어 비유와 질문 문장은 취업 자격으로 사용하지 않습니다. 취업 비자와 수출 허가가 같은 문단에 있어도 문장별로 구분합니다.
- 원격근무 관련 설명과 필터를 **명시된 근무 지역과 거주 국가의 비교**로 바꾸었습니다. 거주 국가를 국적·취업 허가로 간주하지 않고 추가 개인정보를 입력받지 않습니다. 이러한 조건으로 추천 점수를 조정하거나 지원자를 자동 제외하지 않습니다.
- 세 수집기가 같은 해석을 사용합니다. 이전 캐시와 저장 기록은 보관한 본문·비자 근거로 다시 확인하고 공고 ID·원래 조회 시각·저장일·메모·지원 상태를 유지합니다. 취업 자격 정보도 저장 내용 비교에 포함하며 CSV 끝에 비자 지원·자격 조건·원문 근거를 추가합니다.
- 데이터 화면에 취업 자격 원문을 찾은 공고 수를 표시합니다. 원문을 찾지 못한 경우가 제한 없는 조건을 뜻하지 않음을 함께 안내합니다.

### 참고 자료와 판단

- [Schema.org eligibilityToWorkRequirement](https://schema.org/eligibilityToWorkRequirement): 시민권·비자·서류 등의 취업 요건을 나타내는 의미.
- [Google JobPosting의 applicantLocationRequirements](https://developers.google.com/search/docs/appearance/structured-data/job-posting#applicant-location-requirements): 재택근무 지원자의 지리적인 위치 범위.
- [GOV.UK의 취업 권한 증명](https://www.gov.uk/prove-right-to-work), [USCIS의 미국 취업 안내](https://www.uscis.gov/working-in-the-united-states): 거주 위치·비자 지원 정책·개인의 취업 권한은 같지 않다는 구분.
- [22 CFR §120.62, Cornell LII](https://www.law.cornell.edu/cfr/text/22/120.62): 해당 수출 규정의 `U.S. person`에는 영주권자와 보호 대상자도 포함됨.

공식 안내는 데이터 의미를 구분하는 데 참고했습니다. 지원자에게 법적 요건을 자동 적용하지 않으며, 적용 범위와 최신 조건은 회사가 제공한 원문으로 확인하도록 유지했습니다.

### 실제 데이터 확인

2026-09-19 **08:26 UTC**의 배포용 서버에서 17개 게시판이 모두 정상 조회되었고, 탐색 대상 공고 844개를 확인했습니다. 동일한 본문에 이전·변경 후 비자 해석을 적용해 비교했습니다.

| 확인 항목 | 결과 |
| --- | ---: |
| 취업 자격·비자 적용 범위의 원문을 찾은 공고 | 353개 |
| 비자 지원 명시에서 조건부로 바로잡은 공고 | n8n 6개 |
| 국적·시민권 문구를 확인한 공고 | 11개 |
| 기존 취업 허가 문구를 확인한 공고 | 7개 |
| 거주 요건 문구를 확인한 공고 | 35개 |
| 보안 인가 문구를 확인한 공고 | 4개 |
| 수출 통제 문구를 확인한 공고 | 123개 |

종류별 건수는 중복됩니다. 수치는 해당 시점의 844개 공고에서 해석한 범위이며 전체 시장의 비율이나 파서 정확도를 뜻하지 않습니다. n8n의 독일 한정 문구, Anthropic의 필수 시민권·우대 보안 인가, MongoDB·GitLab·Mistral의 시민권·취업 허가 조건과 Airbnb의 주별 제한을 원문과 대조했습니다. 844개 공고 모두 서버의 정규화 결과와 공유 해석이 같고, 게시 상태 API의 표시 항목별 비교 값도 일치했습니다.

### 검증

- 전체 단위 검사 **175개 통과**, 배포용 빌드 성공. 국가 한정 지원, 지원 불가·모순·미확인, 수출 허가와의 구분, 필수·우대·선택 경로, 거주 국가와 취업 자격의 분리, 긴 본문과 근거 제한, 세 출처·캐시·저장 이전과 내용 비교를 확인했습니다.
- 배포용 서버의 전체 브라우저 시나리오 **40개 통과**. 비자 필터, 자격 원문 열람, 저장·메모·지원 상태·재방문·CSV, 이전 저장 공고의 재해석, 320px에서 필수·우대 원문의 접근성을 확인했습니다.
- 실제 n8n·Anthropic 공고로 **1440px·390px·320px** 저장 목록과 **1440px·320px** 상세 화면을 확인했습니다. 펼친 원문과 필수·우대 표시를 직접 검토했고, 확인한 화면에서 가로 넘침·브라우저 실행 오류·자동 WCAG A/AA 검사 위반이 없었습니다.

### 이어서 확인할 항목

- 현재 해석은 영어 공고의 명시적인 문구를 대상으로 합니다. 국가별 이민법·주별 고용 제약·시민권 예외나 보안 인가 발급 가능성을 자동 판단하지 않습니다.
- 조건을 찾지 못했거나 복합 문구의 적용 범위를 확정하지 못하면 원문 확인이 필요합니다. 새로운 언어와 공고 형식을 넓힐 때 실제 원문을 대조해 누락과 오분류를 확인해야 합니다.
